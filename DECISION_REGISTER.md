# Decision Register (Final)

This supersedes the "proposed" decisions in `MASTER_PROJECT_BLUEPRINT.md` Part 37. Every decision below is **final** unless marked `DECISION REQUIRED`. Claude Code should treat this file as binding — do not re-derive or second-guess these choices during implementation. If evidence during implementation genuinely contradicts a decision, stop, document the conflict in a new row here (old decision → new evidence → proposed replacement), and flag it to Rehman before proceeding — do not silently change course.

## Research decisions (carried forward from MASTER_PROJECT_BLUEPRINT.md, now confirmed)

| # | Decision | Final choice | Why | Alternatives rejected |
|---|---|---|---|---|
| R1 | Research direction | Cross-dataset generalization + shortcut/bias analysis, not a plain classifier | A Feb 2026 paper already reports 96.2% in-domain; a July 2026 paper shows the field's benchmarks may be confound-inflated (verified from primary sources) | Plain "train XLM-R, report accuracy" |
| R2 | Primary dataset | Ax-to-Grind Urdu (10,083 articles) | Best-documented, expert-annotated, non-MT, has a verified, directly-relevant length confound to study | Notri-Fact or UFND as primary |
| R3 | Cross-dataset test set | Notri-Fact Urdu (13,388 articles) | Matches the exact pairing in the paper being extended, enabling direct comparison | Hook and Bait (source-leakage risk), Farooq et al. (MT artifacts) |
| R4 | Second transformer | mBERT | The genuine novel extension over the July 2026 paper, which tested XLM-R only | XLNet, LLaMA-2-scale models (disproportionate compute for the research question) |
| R5 | Primary metric | Macro-F1 | Sensitive to the prediction-collapse failure mode central to this thesis; accuracy can mask it | Accuracy, AUC |
| R6 | Explainability method | Integrated Gradients (via `captum`) | Better Colab-budget fit than SHAP; formal completeness axiom is easier to defend in a viva than a sampling-based approximation | SHAP, raw attention weights (explicitly rejected — not reliable as explanations) |
| R7 | No dataset concatenation | Ax-to-Grind and Notri-Fact always stay separate train/test pools | Concatenating would destroy the core zero-shot cross-dataset experiment | Merging for a larger training set |
| R8 | No new dataset/annotation claimed | Uses existing public datasets only | Time budget protected for the REQUIRED experiment set; new annotation is thesis-scale on its own | Manually annotating a Roman Urdu supplementary set |

## Engineering decisions (finalized this phase — supersedes earlier "leaning" language)

| # | Decision | Final choice | Why | Alternatives rejected |
|---|---|---|---|---|
| E1 | Repository structure | **Single monorepo** (`frontend/`, `backend/`, `ml/`, `research/`, `docs/`) | One cohesive research+product project; a student-scale project gains nothing from multi-repo overhead and loses easy cross-referencing between research artifacts and the app that serves them | Separate frontend/backend/ML repos |
| E2 | Frontend framework | **Next.js 14+ (App Router) + TypeScript** | File-based routing removes a whole category of "where does this page go" decisions for the implementing agent; built-in support for both server-rendered content pages (Methodology, About — good for a shareable portfolio link) and client-interactive pages (Analyze); deploys natively and freely on Vercel | Plain React+Vite (weaker default routing/SSR story for content pages that need to look good when shared as a link), pure Streamlit/Gradio (faster to build, but the brief explicitly wants this project to demonstrate software-engineering breadth, not just a notebook-wrapped demo) |
| E3 | Styling | **Tailwind CSS + shadcn/ui primitives, custom design tokens (Part below)** | shadcn/ui ships accessible, unstyled-by-default components (dialog, tabs, cards) that are fully restyleable — avoids both "build a design system from raw CSS" (too slow) and "generic AI-SaaS template look" (shadcn's default look is neutral, not opinionated, so the custom token layer in `FRONTEND_SPECIFICATION.md` fully controls the final identity) | A prebuilt UI kit / template (would fight the "not a generic SaaS template" requirement), Material UI (heavier, harder to make feel bespoke) |
| E4 | Frontend state management | **React built-in state (`useState`/`useReducer`) + a small typed `fetch`-based API client** | The app has no complex cross-page shared state (no auth, no global cart/session-heavy state) — a state library (Redux/Zustand) would be unjustified complexity for this scope | Redux, Zustand, React Query (React Query is a *reasonable* alternative for server-state caching; rejected only because the app's request volume and caching needs are trivial enough that it adds a dependency without a corresponding real benefit — **may be revisited if the History/analytics stretch feature is built**, logged as a conditional future decision, not a rejection of the tool's merit) |
| E5 | Charts | **Recharts** | React-native, integrates cleanly with a Tailwind/shadcn app, sufficient for the confusion-matrix heatmap, benchmark bar charts, and length-distribution histograms this project needs — no need for a heavier viz library (D3, Plotly) given the chart types required are all standard | D3.js (more power than needed, more implementation time), Chart.js (less idiomatic in a React/TS codebase) |
| E6 | Backend framework | **FastAPI + Pydantic v2 + Python 3.12** (interpreter re-pinned from 3.11 by M4-2; the framework choice itself is unchanged) | Native fit with the PyTorch/Transformers/captum ML stack — no cross-language serving layer; async support for I/O-bound URL extraction; automatic OpenAPI docs, which doubles as living API documentation for free | Flask (weaker native async + validation story), Django REST Framework (far more machinery than this project needs — no auth system, no admin panel, no ORM-backed models) |
| E7 | Backend layering | **Routers → Services → ML layer**, Pydantic schemas as the request/response contract | Keeps HTTP concerns (routers) separate from business logic (services) separate from model execution (ML layer) — the specific separation that makes the mandatory preprocessing train/serve-parity requirement (`ML_SPECIFICATION.md`) enforceable in code, not just in a doc | A single-file FastAPI app (fine for a prototype, not for a project meant to demonstrate engineering ability) |
| E8 | Database | **None for the MVP.** Curated example articles ship as a static JSON fixture bundled with the backend; no user accounts, no server-side history (history, if built, is session-local client state only, per `MASTER_PROJECT_BLUEPRINT.md` Part 17/18) | Nothing in the required feature set needs persistent server-side storage; adding a database "because most apps have one" would be exactly the unjustified-complexity the brief warns against | PostgreSQL/Supabase (real overkill), Redis (only relevant if rate-limiting needs to be distributed across multiple backend instances, which a single free-tier HF Space instance does not) |
| E9 | Database — stretch fallback, `DECISION REQUIRED` only if built | **SQLite** (file-based, zero-ops) if a future "aggregate research metrics" feature (e.g., a live counter of analyses run, aggregate confidence distribution) is ever added | Zero operational overhead, no separate service to deploy/pay for, sufficient for read-light aggregate stats | Postgres/Supabase — only justified if the project ever needs concurrent multi-instance writes, which is not anticipated |
| E10 | Rate limiting | **In-process, IP-based sliding window** (e.g., `slowapi`, a FastAPI-native rate-limiting middleware) | Matches the single-instance, no-Redis deployment (E8); sufficient for a free-tier public demo | Redis-backed distributed rate limiting (unjustified given E8) |
| E11 | Article extraction library | **`trafilatura`** | Purpose-built for main-content extraction from arbitrary news HTML, handles multilingual content reasonably, actively maintained, pure-Python (no headless browser, which is itself a security decision — see `SECURITY.md`) | `readability-lxml` (less actively maintained), a headless-browser-based scraper (explicitly rejected — executing arbitrary page JavaScript is a security risk this project does not need to accept) |
| E12 | Deployment split | **Frontend → Vercel (free tier). Backend + model inference → Hugging Face Spaces (Docker SDK, CPU basic tier). Model weights → Hugging Face Model Hub.** | Plays to each platform's actual strength: Vercel is the best-in-class free host for a Next.js app (global CDN, zero-config CI from GitHub); HF Spaces is the best free host for a Python/PyTorch inference service and is where the verified "no Urdu FND demo exists" gap should be filled, since that's where ML-portfolio visitors look first | Two HF Spaces (one static, one backend) — rejected because HF Spaces' static/Node hosting is a worse fit for Next.js than Vercel's purpose-built pipeline; a single self-hosted VM — rejected as unnecessary cost/maintenance |
| E13 | Backend testing | **pytest + `httpx`'s `TestClient`** for API tests; pytest for ML sanity tests; no separate ML test framework needed | Standard, well-understood Python testing stack; FastAPI's own docs recommend this exact pairing, meaning Claude Code has strong, unambiguous conventions to follow | unittest (less ergonomic for fixtures/parametrization at this project's scale) |
| E14 | Frontend testing | **Vitest + React Testing Library** for component/unit tests; **Playwright** for the one required end-to-end flow | Vitest is the natural pairing with a Vite-compiled Next.js/TS toolchain and is faster than Jest; RTL is the standard for behavior-focused component tests; Playwright chosen over Cypress for lighter CI footprint and first-class TypeScript support | Cypress (heavier CI setup), Jest (slower, more config for this stack than Vitest) |
| E15 | CI/CD | **GitHub Actions**: one workflow running backend tests + lint, one running frontend tests + lint, triggered on every PR to `main`. Deployment itself is via each platform's native GitHub integration (Vercel auto-deploys on push to `main`; HF Spaces is deployed via a documented `git push` to the Space's own git remote, either manually or via a simple Actions step) | Free, minimal-setup, matches the free-tier-only constraint; avoids building a custom deploy pipeline that would need secrets management disproportionate to this project's risk profile | A dedicated CD tool (Argo, etc. — absurd for this scale) |
| E16 | Config/secrets | **`pydantic-settings`** on the backend (typed env-var loading); Vercel's built-in environment-variable dashboard on the frontend, exposing only the public API base URL (`NEXT_PUBLIC_API_BASE_URL`) — **no secret ever needs to live in the frontend**, since the backend has no auth requiring a shared secret | — | A `.env`-parsing library with less type safety |
| E17 | Model checkpoint storage | **Hugging Face Model Hub** (public model repo), loaded by the backend at container startup via `transformers`' standard `from_pretrained` | Free, versioned, directly integrates with the deployment target (E12), and is itself a portfolio-visible artifact | Storing checkpoints in git (explicitly wrong — large binary files in git history), a private cloud bucket (adds cost/credentials for no benefit given the model has no reason to be private) |
| E18 | Shared-preprocessing packaging across `research/` and `backend/` | **The backend Docker build context is the repository root, not `backend/`.** The Dockerfile (`backend/Dockerfile`, invoked with root context) `COPY`s both `backend/app/` and `research/src/data/clean.py` into the image. `research/src/data/clean.py` is constrained to pure-stdlib text transforms (`re`, `unicodedata` — no `pandas`/`torch`/heavy deps) specifically so it copies cleanly without pulling research-only dependencies into the serving image | Found during the pre-coding audit: a per-directory Docker build context (`backend/` only) cannot reach a file outside it, which would have silently broken the train/serve preprocessing-parity requirement (`ML_SPECIFICATION.md` Section 9, `CLAUDE.md` rule 4) the first time someone tried to build the image | A second copy of the cleaning logic inside `backend/` (explicitly rejected — this is exactly the drift `CLAUDE.md` rule 4 exists to prevent); a standalone installable shared package (`packages/text_cleaning/`) — viable, but rejected as more machinery than a single-file, dependency-light module needs at this project's scale |

## Remaining unresolved decisions

Two items are genuinely irreducible — they depend on data or measurements that don't exist yet and cannot be produced without violating this phase's "no training, no downloading" constraint. Everything else has been resolved during this audit (see below).

| # | Decision | Why it's genuinely open | Resolution path |
|---|---|---|---|
| U2 | Exact max sequence length for tokenization | ~~Depends on the empirical length distribution from the dataset audit, not yet run~~ | **RESOLVED 2026-08-13 at Milestone 2 → see row M2-1 below. Final value: 512.** |
| U4 | Whether model quantization (fp16/int8) is needed for the HF Spaces free CPU tier | Depends on measured latency/memory once the real model is deployed, not assumable in advance | `DECISION REQUIRED` — measured and decided during Milestone 9 (deployment), documented as a new row here once resolved |

**Resolved during this audit (were previously open):**

- **U1 (UFND dataset link) — now CLOSED, not pursued further.** Two independent research passes (Phase 2 and this audit) both failed to locate a direct download link via the paper's Data Availability Statement or any repository search. Continuing to search would not be a good use of implementation time. **Decision: UFND is excluded from the active dataset plan.** `research/data/raw/ufnd/` and its references in `DATASET_PLAN.md`/`ARCHITECTURE.md` remain as an optional path only — Milestone 1 should make one final, time-boxed (≤30 min) attempt, and if still unresolved, proceed with Ax-to-Grind + Notri-Fact only, without further escalation. This is now a locked decision, not an open question.
- **U3 (React Query) — folded into E4.** E4 already states the MVP uses built-in React state; React Query is correctly framed as "reconsider only if the History/analytics stretch feature (Milestone 11) is actually built," which is a conditional-but-resolved decision, not a genuinely open one. No standalone entry needed.

Claude Code: treat U2 and U4 as hard stops — do not guess. Resolve each using its stated resolution path and document the outcome as a new row in this table when the underlying milestone produces the needed evidence. **U2 is now resolved (M2-1); U4 remains open.**

## Implementation-phase decisions (logged as milestones produce evidence)

| # | Date | Decision | Evidence | Status |
|---|---|---|---|---|
| M1-1 | 2026-08-13 | **U1 (UFND) final attempt executed — link NOT found. UFND stays excluded.** | Fetched the Data Availability Statement of *"Verifying Urdu news authenticity using deep learning with concatenated BERT and GloVe embedding"* (Feroz, Abbasi, Babar, Aljohani), `nature.com/articles/s41598-026-36771-0`. It states verbatim: *"The dataset is open-source and is used in compliance with their respective data usage policies and it can be accessed from https://github.com/Sheetal83/Ax-to-Grind-Urdu-Dataset.git and https://zenodo.org/records/7773474"*. Neither link is a 14,178-article UFND corpus: the first is Ax-to-Grind (this project's primary dataset, 10,083 rows); the second is a Zenodo record titled *"Data for Urdu fake news"* (DOI 10.5281/zenodo.7773474, CC-BY-4.0, single `news.xlsx`, 1.9 MB), which was downloaded and inspected — it holds **4,097 data rows** with columns `news` / `label`, not 14,178. | **CLOSED — not found.** Proceeding with Ax-to-Grind + Notri-Fact only, per U1's prescribed path. Time-box respected; no further search authorised. |
| M1-2 | 2026-08-13 | **Observation only, NOT acted upon:** the paper's own DAS points exclusively at Ax-to-Grind (10,083) + the Zenodo record (4,097). Those sum to 14,180, against UFND's reported 14,178. | Arithmetic on the two row counts verified above. This is a numerical coincidence consistent with UFND being a concatenation that includes Ax-to-Grind, which — if true — would mean the Feb 2026 paper's 96.2% is partly in-domain on this project's own training set. **It is a hypothesis, not a finding: nothing here confirms it**, and no attempt was made to verify it. | **Noted for the thesis's related-work discussion.** No pipeline change. Requires real verification before it may be stated anywhere as fact (`CLAUDE.md` rule 2). |
| M1-3 | 2026-08-13 | **`research/data/raw/ax_to_grind/Fake News.csv` is corrupt upstream and cannot be used as a text source. `Combined .csv` from the same repository is downloaded alongside it as the only intact source of fake-labelled Urdu text.** | Measured on the pinned commit `217d3fa`: `Fake News.csv` is tab-separated, is not valid UTF-8, and despite its name contains the **full** dataset (5,053 FAKE + 5,030 TRUE), not the fake half. **2,694,909 of its 3,664,112 bytes are the literal character `?` (0x3F)** and only 27 Arabic-script characters survive — a lossy encoding conversion upstream destroyed the Urdu irrecoverably. `True News.csv` (comma-separated, UTF-8+BOM) is intact: 5,030 TRUE rows, 633,940 Arabic-script characters. `Combined .csv` (comma-separated, UTF-8+BOM) is intact and holds 5,053 FAKE + 5,030 TRUE = **10,083 rows**, exactly the size `DATASET_PLAN.md` documents. | **DECISION REQUIRED — Rehman to confirm before Milestone 2.** Milestone 1 only acquires and checksums all three files; it does not choose which one the pipeline reads. Proposed resolution: `validate.py`/`clean.py` read `Combined .csv` as the sole Ax-to-Grind source, with `True News.csv` used as an independent cross-check of the TRUE half, and `Fake News.csv` retained purely as the provenance record of the corruption. `DATASET_PLAN.md` Section 1 must be updated in the same commit that implements this.<br><br>**Re-verified 2026-08-13 — CONFIRMED, finding stands.** Rehman correctly noted that re-running `download.py` would re-fetch the same content-addressed blob and prove nothing about a one-off transfer error, so three checks on genuinely different code paths were used instead. **(a) Object-identity, no git involved:** the git blob SHA-1 of each local file was computed in pure Python as `sha1(b"blob <len>\0" + bytes)` and compared with the blob `sha` the GitHub Contents API reports at `?ref=217d3fa…`. All three match exactly — `Fake News.csv` = `7736a86a68e9ded3fc42580a411d0e01e6931e09`, `True News.csv` = `85c2e496fa08b6d3919f23020632c55be983f881`, `Combined .csv` = `597a9f1a8974558b2b152956be5c514ed84d0392` — proving the downloaded bytes *are* the exact objects in GitHub's store, so no transfer error occurred. (An initial mismatch was traced to `git hash-object` applying Windows CRLF filtering; `--no-filters` reproduces the API values exactly.) **(b) Independent transport:** a separate `git clone` over the git/smart-HTTP protocol, with `git ls-tree 217d3fa…`, independently reproduces the same blob→path mapping. **(c) Independent content check:** blob bytes extracted via `git cat-file blob` — a completely different path from the original `requests.get` against `raw.githubusercontent.com` — are byte-identical to the downloaded files, and still contain **2,694,909 literal `?` (0x3F) bytes, 73.5% of the file**, only 27 Arabic-script characters, and fail strict UTF-8 decoding at byte 501. The corruption is genuinely upstream in the published dataset, not an artefact of this project's download. |
| M2-1 | 2026-08-13 | **U2 RESOLVED — max sequence length = 512 subword tokens (serving only).** Supersedes the provisional 384. | Measured with the real `xlm-roberta-base` tokenizer over Tier-2-cleaned text (`research/data/audit/dataset_quality_report.json`). Subword percentiles — Ax-to-Grind (n=10,083): median 40, p90 200, p95 496, p99 1,045, max 1,447. Notri-Fact (n=13,388): median 201, p90 306, p95 349, p99 482, max 2,217. Untruncated coverage: 256 → 91.5% / 76.6% (combined 83.0%); 384 → 93.8% / 97.0% (95.6%); **512 → 95.3% / 99.2% (97.5%)**. 512 is XLM-R's architectural ceiling (512 position embeddings), so nothing larger exists. Serving input is a pasted news article, which resembles Notri-Fact's full articles rather than Ax-to-Grind's headline-length snippets (median 40 tokens), making 99.2% the representative figure. | **CLOSED.** `ML_SPECIFICATION.md` Section 3 updated. Costs ~1.8x the attention of 384 on free-tier CPU; if Milestone 9 measures a breach of the <2 s target, the documented order is resolve U4 (quantization) first, then fall back to 384 (95.6% combined). Below 384 is not acceptable — 256 truncates 23% of full-article-shaped input. `app/config.py` must match at Milestone 6. |
| M2-2 | 2026-08-13 | **Ax-to-Grind has no domain/source/publisher/author/date field. Source-leakage risk is UNTESTABLE, not absent. `split.py` uses plain stratification, not grouped splitting.** | The released data has exactly three columns: `Sr. No.`, `News Items`, `Label`. The 15 domains described in the source paper are not present in the distributed files. | **CLOSED as a documented limitation.** Part 7 item 4's source cross-tab cannot be computed and the Risk Register's source-leakage row stays unresolved-by-necessity for this dataset. Blueprint Part 9's grouped-splitting option is unavailable; Part 9's temporal-holdout option is likewise impossible (no date field). Both must be stated as limitations in the thesis rather than reported as clean. Notri-Fact does carry `Category` and `Date`. |
| M2-3 | 2026-08-13 | **Surface-form mismatch between the two corpora — flagged for a methodology decision, NOT acted on.** | Notri-Fact contains zero non-alphanumeric characters across the whole corpus — no Urdu full stop, comma or question mark — having evidently been punctuation-stripped before release. Ax-to-Grind retains 53 distinct non-alphanumeric characters (8,356 Urdu full stops, 6,581 commas, …). Ax-to-Grind's punctuation density also differs by label: real 0.0147 vs fake 0.0102. | **DECISION REQUIRED — Rehman, before Milestone 5's cross-dataset experiments.** A punctuation-linked cue learned on Ax-to-Grind is systematically unavailable on Notri-Fact, so an RQ2 F1 collapse is confounded by surface form as well as by domain and length shift. Options: (a) leave as-is and report the confound honestly; (b) strip punctuation from both corpora so the surfaces match, at the cost of removing a potentially genuine signal; (c) run both and report the delta as its own finding. Changing this alters research methodology, so rule 3 requires it be decided and logged here before implementation — Milestone 2 deliberately does not choose.<br><br>**RESOLVED 2026-08-13 — Rehman chose option (c): run both, report the delta as its own finding.** Logged as new experiment **Q** in `EXPERIMENT_PLAN.md` Section 2, ordered immediately after step 4 (F, G) since it exists to correctly interpret F/G's headline cross-dataset result, not as an optional extension — treated as part of the REQUIRED set for that reason. Design: after F/G's standard Ax-to-Grind→Notri-Fact zero-shot transfer (original surface form on both sides), retrain on a punctuation-stripped copy of Ax-to-Grind's training split and re-evaluate zero-shot on Notri-Fact (already punctuation-free as released); report the macro-F1 delta between F/G's result and Q's as a standalone finding isolating the punctuation confound from the length (M2-1-adjacent finding) and domain-shift (M2-2) confounds already logged. Scope proposed during this review, open to correction: **XLM-R only** (the primary model, not all 4), **Ax-to-Grind→Notri-Fact direction only** (the direction where the confound actually applies — training exposed to a punctuation-label correlation, testing on a corpus with none; the reverse direction doesn't exercise the same mechanism), 3 seeds (consistent with every other transformer run informing a headline result). Kept deliberately narrower than F/G's full 4-model × 2-direction matrix to stay proportionate to the project's time budget (R8) — this is a targeted confound-isolation check, not a second full cross-dataset experiment. |
| M3-1 | 2026-08-14 | **Experiment H's specified linear model understates the length shortcut, because the length↔label relationship is NON-MONOTONIC. Proposing a nonlinear length-only variant be added to the plan. Not acted on.** | H as specified (`MASTER_PROJECT_BLUEPRINT.md` Part 32, "a linear model on word count alone") scores **macro-F1 0.6145** on Ax-to-Grind test, against a majority-class floor of 0.3333 and content-based baselines A=0.8755 / B=0.8835. But the raw `n_words` ROC-AUC is **0.4995 — indistinguishable from chance** — while the top length decile (166–1,088 words) is **100% fake**. Both hold because the relationship is U-shaped: measured fake-rate by `n_words` decile runs 75.2%, 71.4%, 61.6%, 37.7%, 32.1%, 20.3%, 15.2%, 14.9%, 76.6%, 100.0%. Fake articles sit at BOTH extremes; real articles occupy a narrow middle band (~20–57 words). A linear model cannot express "inside or outside a band", so it structurally cannot use this. A depth-2 decision tree on the same two features — no content, same information — reaches **macro-F1 0.7552**, and depth-5 reaches **0.7625**, i.e. ~86% of experiment B's content-based score. Diagnostic recorded in `research/results/metrics/H_length_only_ax_to_grind_*.json` under `run_metadata.length_relationship_diagnostic`. | **DECISION REQUIRED — Rehman, before Milestone 5's RQ3 write-up.** The risk is interpretive: reading H=0.61 as "length explains only a little" would materially understate the confound and inflate how much of C/D's performance looks like genuine linguistic signal. The honest reading is that length-only predictive power on this dataset is ~0.76, and H's linear number is a floor. Options: (a) keep H linear as specified and report the non-monotonicity diagnostic alongside it as the interpretive caveat; (b) add a nonlinear length-only variant (**H2**, shallow decision tree, same length-only feature registry) as an additional REQUIRED experiment and report both, making the floor/ceiling explicit; (c) replace H's classifier outright — not recommended, as it breaks comparability with Part 32's stated design. Milestone 3 deliberately does not choose: adding or changing an experiment is a methodology change and rule 3 requires it be logged and decided here first. Recommended: **(b)**, since it costs one cheap deterministic run and converts an interpretive caveat into a measured number.<br><br>**RESOLVED 2026-08-14 — Rehman approved option (b).** H2 (shallow decision tree — depth capped, same length-only feature registry as H: `n_chars`, `n_words`, no content access) is added to `EXPERIMENT_PLAN.md` as a new REQUIRED experiment, run immediately alongside H at the same step. H itself is unchanged, per Part 32's stated design; H2 supplies the ceiling number so RQ3's write-up can report floor (H, linear) and ceiling (H2, nonlinear) as two measured numbers rather than one number plus an interpretive caveat.<br><br>**IMPLEMENTED 2026-08-14.** `run_shortcut_analysis.py --mode length-only --classifier decision_tree`, results committed to `research/results/metrics/H2_length_only_ax_to_grind_{val,test}.json`. **Reported config: `max_depth=2`** — chosen over depth 5 because depth 2 is the *minimum* capacity able to express "inside or outside a band" (one split per band edge), so its score cannot be attributed to a high-capacity model memorising the training split, and depth 5's measured gain over it is only +0.0073 macro-F1, which is itself evidence that the band structure is the whole of the available signal. A conservative ceiling makes the claim H2 supports ("length alone reaches ~0.76") harder to dismiss. Depth 5 is still measured and recorded under `run_metadata.depth_sweep_diagnostic` so the choice stays auditable. **Measured, seed 42: test macro-F1 = 0.7552 (val 0.7430) against H's 0.6145 (val 0.5744) and a majority-class floor of 0.3333 — a floor→ceiling gap of +0.1408 on test.** Depth-5 probe: test 0.7625, val 0.7480. Both reproduce M3-1's original diagnostic figures exactly. The fitted depth-2 tree independently confirms the U-shape by a different route than the per-decile rates that motivated this row: it splits `n_words <= 70.5` → (`n_chars <= 104.5` → fake, else **real**) and `n_words > 70.5` → fake, i.e. it carves out a middle band as real and labels both extremes fake. The pair is written side by side into H2's `run_metadata.length_shortcut_floor_ceiling`, and a schema test cross-checks it against H's committed file so the two can never silently disagree. |
| M4-1 | 2026-08-14 | **Training and evaluation tokenise at `max_length=512`. "Full, uncapped text" (blueprint Part 8) is not architecturally expressible, so 512 is the only possible reading. Factual documentation, NOT a judgment call — the architecture leaves no alternative.** | Both mBERT and XLM-R-base have 512 learned position embeddings and cannot accept a longer sequence at any batch size or on any hardware; the ceiling is architectural, not a budget choice. M2-1 already established 512 for *serving*; this row records that the same ceiling necessarily binds *training* too. Measured impact at 512 over Tier-2-cleaned text: **Ax-to-Grind 478/10,083 rows truncated (4.74%), max observed 1,447 tokens; Notri-Fact 113/13,388 (0.84%), max 2,217.** **The Ax-to-Grind truncation is entirely label-asymmetric: all 478 truncated rows are FAKE — 9.46% of the fake class and 0.00% of the real class.** Notri-Fact's is near-symmetric by comparison (91 real, 22 fake). Each metrics file records its own run's figures under `run_metadata.truncation`. | **CLOSED as documentation.** Three consequences the thesis must state rather than discover late. (1) RQ3's transformer results describe **genuine signal within a 512-token window**, not exposure to the full observed length range — the phrase "the model learned X from article length" is only defensible up to that window. (2) The truncation is **correlated with the label**, so it is not neutral preprocessing: clipping only the longest fake articles partially attenuates the very confound RQ3 measures, while simultaneously making "this article hit the 512 cap" a near-perfect fake indicator in this training set — the shortcut is transformed, not removed. (3) C/D are therefore **not directly length-comparable to H**, which used uncapped `n_chars`/`n_words`; any statement comparing transformer performance against the length-only baseline must note that the two saw different length information. No mitigation is available or attempted: a longer context would require a different architecture (e.g. Longformer), which R4 does not permit without a new decision row. |
| M4-2 | 2026-08-15 | **The Python 3.11 pin is already violated by every environment this project actually runs in — including by the repo's own pinned dependencies. Reported, NOT resolved either way.** | `REPRODUCIBILITY.md` Section 1 pins "Python 3.11 ... this exact minor version", and `backend/pyproject.toml`, `research/pyproject.toml` (`requires-python = "==3.11.*"`) and `.github/workflows/backend-ci.yml` (`python-version: "3.11"`) all agree. Nothing runs on it. **Observed, measured, not assumed: local dev = 3.13.3** (recorded in `run_metadata.python_version` of all 8 committed Milestone 3 metrics files — A, B, H, H2); **Colab = 3.12** (Rehman's real T4 session, 2026-08-14). **The binding blocker is not preference but packaging: `numpy==2.5.2`, pinned in `research/requirements.txt`, declares `requires_python >=3.12` and publishes no cp311 wheel at all** (verified against PyPI metadata — wheels exist for cp312/cp313/cp314/cp315 only). `pip install -r research/requirements.txt` therefore **cannot succeed on Python 3.11**, so the repo's own pinned dependency set contradicts the repo's own declared interpreter. `torch==2.13.0`, `pandas==3.0.5` and `scikit-learn==1.9.0` do ship cp311 wheels; numpy alone is the constraint. **Forcing 3.11 inside Colab was investigated and rejected as impractical**, on two independent grounds: (1) it is moot — a 3.11 venv still could not install the pinned requirements without unpinning numpy, which is itself a Section 1 violation; (2) `google.colab` (Secrets, file download — used by the Milestone 4 notebook) exists only in Colab's system Python, so a 3.11 venv would leave notebook cells on 3.12 and only subprocesses on 3.11, i.e. a split environment that is *less* reproducible than the single 3.12 one, before touching the CUDA-build coupling. | **DECISION REQUIRED — Rehman, before Milestone 5.** Changing an interpreter pin is a `REPRODUCIBILITY.md`-locked constraint, so `CLAUDE.md` rule 3 forbids silently accepting the drift *and* rule 13 forbids silently enforcing 3.11. Nothing has been changed. Why it matters for validity rather than tidiness: the classical baselines (A, B, H, H2) were computed on 3.13.3 and the transformer runs (C, D) will be computed on Colab's 3.12, so the headline comparison RQ1 and RQ3 rest on spans two interpreters and two numpy/BLAS builds. The expected numeric effect is small but is not zero and is currently unmeasured. Options: **(a)** re-pin to `>=3.12,<3.14` across `REPRODUCIBILITY.md`, both `pyproject.toml`s and CI, documenting the real per-environment versions — cheapest, and makes the docs true; **(b)** pin exactly 3.12 (Colab's version) and re-run Milestone 3's four classical experiments on it so every committed number comes from one interpreter — costs one cheap deterministic re-run and removes the ambiguity entirely; **(c)** hold 3.11 and downgrade the numpy/pandas pins to 3.11-compatible releases, re-running everything — most faithful to the current spec, most expensive, and Colab cannot natively provide 3.11 so the Milestone 4/5 GPU work would still diverge. Recommended: **(b)**, since it is the only option under which every committed metric in the thesis comes from a single, stated interpreter, and the re-run it costs is minutes. Not acted on.<br><br>**RESOLVED 2026-08-15 — Rehman approved option (b).** Pin exactly Python 3.12 (matching Colab, and matching what `research/requirements.txt`'s own numpy pin actually requires) across `REPRODUCIBILITY.md` Section 1, `backend/pyproject.toml`, `research/pyproject.toml`, and `.github/workflows/backend-ci.yml`. Re-run Milestone 3's four classical experiments (A, B, H, H2) on 3.12 and overwrite their committed metrics files so every number in the REQUIRED set traces to one stated interpreter, removing the cross-environment ambiguity entirely. `backend/`'s own code is unaffected by this numpy constraint (it has no numpy dependency yet), but the pin is project-wide per `REPRODUCIBILITY.md`, so it moves too.<br><br>**IMPLEMENTED 2026-08-19.** Pin moved to `==3.12.*` in `REPRODUCIBILITY.md` Section 1, `backend/pyproject.toml` (plus `ruff target-version = "py312"` and `mypy python_version = "3.12"`), `research/pyproject.toml` and `.github/workflows/backend-ci.yml`; the docs that quoted the old pin (`ARCHITECTURE.md`, `BACKEND_SPECIFICATION.md`, `DEPLOYMENT_PLAN.md`, both `requirements.txt` headers, E6 above) were synced in the same commit per rule 8. **A, B, H and H2 re-run on CPython 3.12.11 with the package pins held exactly constant (numpy 2.5.2, scikit-learn 1.9.0, pandas 3.0.5), so the interpreter was the only variable that moved. Every numeric leaf in all 8 committed metrics files is bit-identical to the 3.13.3 values** — verified by walking the full JSON tree, not only comparing headline macro-F1; largest absolute delta across all numeric leaves = 0. Headlines unchanged: A test 0.8755, B test 0.8835, H test 0.6145, H2 test 0.7552 against a 0.3333 majority-class floor, so M3-1's floor→ceiling gap of +0.1408 reproduces exactly and no downstream claim moves. The only non-numeric change: H's two files gained `run_metadata.classifier`, because they were last written at `a1a2634`, before `61782f1` added that field — the committed copies were stale against the current writer, and regenerating them closed that drift. `backend/` was verified rather than assumed on 3.12: clean install of `requirements.txt`, 2 tests passed, `ruff check`, `ruff format --check` and `mypy --strict` all clean. The Milestone 4 notebook's environment gate now asserts `3.12` at run time, so a future GPU-platform drift cannot silently reintroduce the two-interpreter ambiguity this resolution removed. |
| M4-3 | 2026-08-19 | **Milestone 4's GPU run moves from Colab to Kaggle. Platform portability only — no research-design element changed. Reported for the record, and because one Kaggle default would silently change training if left alone.** | Colab's free GPU quota was exhausted on 2026-08-19 (a usage-limit error, confirmed live in the browser — not a bug and not a code fault). Rehman is switching to Kaggle, where he already holds working credentials from Milestone 1. Kaggle differs from Colab in exactly four ways this pipeline touches, and each is handled rather than worked around: (1) **secret store** — `kaggle_secrets.UserSecretsClient` instead of `google.colab.userdata`; (2) **working directory** — `/kaggle/working`, since `/content` does not exist there and `/kaggle/working` is both the only roomy path and the only one exposed in the Output tab; (3) **internet is OFF by default** and must be toggled on, without which the git clone, the `pip install` and the HF checkpoint push all fail separately and several cells apart; (4) **file hand-back** — Kaggle has no `files.download` equivalent, so the metrics zip is collected from the session's Output tab. These live in the new `research/src/notebook_env.py`, so the notebook stays ONE document for both platforms rather than forking into a second copy that would drift. **Interpreter checked, not assumed: Kaggle's current notebook image is built `FROM` the Colab runtime image and ships Python 3.12**, so M4-2's fresh 3.12 pin holds on Kaggle unchanged — and Kaggle inherits Colab's preinstalled torchvision, so the existing uninstall step is still required there. **The one substantive hazard: Kaggle's default accelerator is "GPU T4 x2".** With two GPUs visible, the HF `Trainer` transparently wraps the model in `nn.DataParallel` and reads `per_device_train_batch_size: 16` as *per device* — making the effective batch **32** and cutting the **401 optimizer steps/epoch** that the runtime estimate and the 3-seed design rest on down to ~201. That is a change to the training dynamics, and it would have happened silently and been invisible in the committed metrics. | **CLOSED as a portability fix; no methodology change, so rule 3 is not engaged.** The frozen elements are untouched: training loop, model configs, batch size, 3-seed design, `max_length=512`, local JSON logging and the staging-checkpoint push are all byte-identical. The T4-x2 hazard is neutralised by setting `CUDA_VISIBLE_DEVICES=0` in the notebook's pre-flight cell — restricting GPU *visibility* is what **preserves** the single-T4 design the configs were written for, as against editing a config to compensate, which would have been a methodology change requiring its own row. Selecting P100 (a single GPU) is equally valid. Verified without a Kaggle session: 19 unit tests inject stubs for both platform packages and cover the detection, the Colab→Kaggle fallback order, both empty/missing-secret paths and the per-platform path resolution; the notebook's own cells were then executed against the real module under a simulated Kaggle environment; the CPU dry-run was re-run end to end and is unchanged. **Still unverified, and the reason the next step is a real Kaggle session: the live `kaggle_secrets` call itself**, which cannot be exercised outside Kaggle. The Colab path is kept working and documented rather than deleted, since the quota refills.<br><br>**CORRECTION 2026-08-19, from the first live Kaggle session — the platform detection was wrong on exactly the point this row got right elsewhere.** `detect_platform()` checked `import google.colab` first and treated success as proof of Colab, on the explicit but false premise that no environment provides both platforms' helper packages. That premise contradicts this row's own central finding: because Kaggle's image is built `FROM` the Colab runtime image, it inherits Colab's site-packages, so **`import google.colab` succeeds on Kaggle while being entirely non-functional there** — the same present-but-nonfunctional category as the inherited torchvision copy recorded above. On a live Kaggle P100 the notebook reported `detected platform: colab`, and `google.colab.userdata.get()` hung and raised Colab's own TimeoutException ("Secrets can only be fetched when running from the Colab UI"), wrapped in Colab-specific instructions that were actively misleading on Kaggle. **The inheritance is one-directional, so detection must be Kaggle-first**: a Kaggle-positive signal is conclusive, while a Colab-positive one is only meaningful once Kaggle has been ruled out. Fixed to key off `KAGGLE_KERNEL_RUN_TYPE` / `/kaggle` — the same signal the notebook's own pre-flight cell had been using successfully three cells earlier — with `COLAB_*` env vars (which do not survive into Kaggle's image) as the trustworthy Colab signal and `google.colab` demoted to the weakest one. Two further consequences of the same misdetection, silent and now covered by tests: `working_root()` returned `/content` on Kaggle (where the metrics zip would have been unretrievable) and `deliver_file()` attempted a browser download that does not exist there. **Process note worth keeping:** the original 19 unit tests passed throughout, because one of them (`test_colab_wins_when_both_present`) asserted the wrong precedence as correct — mocks encoded the same false assumption as the code, so they could not falsify it. The replacement cases were verified to FAIL against the old ordering before being accepted. |
| M4-4 | 2026-08-19 | **The Milestone 4 GPU run failed at `from transformers import Trainer` with an ImportError inside numpy. The obvious reading — a scipy/numpy ABI mismatch, parallel to the torchvision case — is WRONG, and the fix that reading implies (uninstall scipy) would have broken the run. Environment/engineering bug; no research methodology touched.** | Kaggle P100, notebook revision 5, repo at `02e86c0`. Everything up to this point succeeded (platform detected as Kaggle, internet on, `CUDA_VISIBLE_DEVICES=0`, secrets resolved, repo synced, stale-notebook guard passed). The failure: `ImportError: cannot import name '_center' from 'numpy._core.umath' (/usr/local/lib/python3.12/dist-packages/numpy/_core/umath.py)`, at the end of a 12-frame chain running `transformers.trainer` → `modeling_utils` → `loss/loss_for_object_detection.py` → `scipy.optimize` → scipy's `array_api_compat` shim → `from numpy import *`. The chain names scipy and an object-detection loss module, which invites the same diagnosis as the inherited torchvision copy above.<br><br>**SUPERSEDED IN PART BY M4-5 (read that row first): the conclusion below that the mixture was *on disk* is WRONG — it is a stale in-memory numpy in the notebook kernel. Everything else in this row, including why scipy must not be uninstalled, still stands.** **Verified independently, and it is a different mechanism.** (1) `numpy/_core/umath.py` is a pure-Python shim that re-exports from the **compiled** `_multiarray_umath` extension; `_center` is a `numpy.ufunc` living in that binary, not a Python-level name. (2) A clean numpy 2.5.2 **does** export it — confirmed on a local 3.12 environment with the project's exact pins. (3) Deleting that single symbol from a healthy install **reproduces the Kaggle message verbatim**. Therefore the failing import is *inside numpy*, not at a scipy/numpy boundary: numpy's `.py` files were 2.5.2 while the compiled extension sitting beside them came from an older numpy — a partial in-place upgrade over the base image's copy, leaving a mixture in `dist-packages`. (4) scipy is only the **messenger**: `numpy.char` and `numpy._core.strings` are lazily loaded, so plain `import numpy` succeeds on a broken install; `from numpy import *` forces them, and scipy's shim is the first thing in the process to run it — which is exactly why every earlier cell, and `import torch` itself, worked. (5) On version compatibility there was never a conflict to begin with: scipy 1.18.0 declares `numpy<2.8,>=2.0.0`, which `numpy==2.5.2` satisfies. | **CLOSED as an environment fix; `CLAUDE.md` rule 3 not engaged — no experiment, config, seed or metric changed.**<br><br>**Uninstalling scipy was considered and rejected outright**, because it would have broken the run rather than fixed it: `import sklearn.metrics` — the path *every* metrics file this project writes goes through (`research/src/evaluation/metrics.py`) — pulls in **493 scipy modules**, and scikit-learn 1.9.0 declares `scipy>=1.10.0` as a hard runtime requirement. scipy is unreachable *dead code* only in the object-detection branch that appears in the traceback; it is very much live on this pipeline's own metrics path. This is the point where the torchvision analogy breaks down: torchvision genuinely is unused here, scipy is not.<br><br>Fix, in `research/notebooks/04_transformer_training.ipynb`: a step 3 in the dependency cell, `pip install --force-reinstall --no-deps numpy==2.5.2 scipy==1.18.0`. `--force-reinstall` rewrites every file of both packages instead of skipping them as "already satisfied", which is what repairs a mixed on-disk install; `--no-deps` keeps it surgical so the rest of the resolved set is untouched. The environment gate now runs `from numpy import *` as a one-second reproducer **before** the `Trainer` import, so a recurrence fails with a message naming the real cause and the fix, rather than a long traceback pointing at the wrong library. Verified in fresh subprocesses in both directions: silent on a healthy numpy, actionable `SystemExit` on a sabotaged one.<br><br>**`scipy==1.18.0` is now pinned in `research/requirements.txt`.** It was executed heavily but left to transitive resolution, which is itself a `REPRODUCIBILITY.md` Section 1 violation ("all Python package versions pinned exactly") and is what allowed the image's preinstalled copy to survive alongside a freshly installed numpy. The three pins (numpy, scipy, scikit-learn) were verified to resolve together cleanly on 3.12.<br><br>**The numpy pin is UNCHANGED and `M4-2` is untouched.** 2.5.2 is not at fault — it works correctly in a clean 3.12 environment; the defect was in how it was installed on top of the image, not in the version chosen. Flagged explicitly because changing that pin would have required revisiting M4-2's resolution.<br><br>`NOTEBOOK_REVISION` 5 → 6, so an already-open tab is caught by the stale-notebook guard. |
| M4-5 | 2026-08-19 | **The same `_center` ImportError recurred on a genuinely fresh Kaggle kernel, but reporting numpy 2.0.2 while pip reported 2.5.2 installed. That single fact disproves M4-4's stated mechanism. The real cause is a stale *in-memory* numpy, not a damaged *on-disk* one — and it also means the training runs were never actually at risk.** | Kaggle P100, notebook revision 6, repo at `532cc10`, cold kernel after a forced session restart. Everything upstream passed. `pip install --force-reinstall --no-deps numpy==2.5.2 scipy==1.18.0` completed, and pip's own dependency-conflict warnings independently confirmed numpy 2.5.2 as installed (`ydata-profiling ... but you have numpy 2.5.2`). The very next cell then failed with `numpy 2.0.2 is installed but INTERNALLY INCONSISTENT: cannot import name '_center' from 'numpy._core.umath'` — **2.0.2 being the base image's version, not the 2.5.2 pip had just written.**<br><br>**Two hypotheses were on the table and both were wrong.** A *shadow install* (numpy present in two trees, `--force-reinstall` touching the one that does not win import resolution) and M4-4's *partially-upgraded copy on disk*. Neither survives the version mismatch: pip and the kernel disagreeing while pip is demonstrably correct is not something either explanation predicts.<br><br>**Root cause, reproduced locally rather than argued from the traceback.** (1) A clean numpy **2.0.2 imports fine** — its own `strings.py` never requests `_center`, so 2.0.2 is self-consistent and cannot fail this way on its own. Therefore the `strings.py` that *does* request `_center` must come from 2.5.2, i.e. from a **newer** numpy than the one reporting its version. (2) Importing numpy 2.0.2 and then repointing `numpy._core.__path__` at a real 2.5.2 installation — old module objects in memory, new files on disk — **reproduces the Kaggle error exactly, including reporting 2.0.2 as `numpy.__version__`.** The mechanism: the hosted image imports numpy at kernel boot; `pip` then correctly replaces it on disk; the kernel keeps the module objects it already holds. `numpy.char` and `numpy._core.strings` are **lazily** loaded, so the first `from numpy import *` after the install reads those two files from the **new** numpy while `numpy._core.umath` is still the **old** cached one, and the new `strings.py` asks for a ufunc the old compiled extension does not export. pip and the kernel are both telling the truth simultaneously. | **CLOSED as an environment fix; `CLAUDE.md` rule 3 not engaged. Logged separately from M4-4 rather than folded into it, because the mechanism is different even though the symptom string is identical — and because M4-4's on-disk explanation is now known to be wrong and must not be relied on.**<br><br>**The most consequential finding is that the training was never at risk.** Every real step already runs as its own `python -m ...` process — `run_in_domain`, `transformer --dry-run`, the data checks — so each one starts a fresh interpreter and reads the freshly installed packages correctly. The **inline environment gate was the only thing in the notebook exposed to the kernel's stale module state**, and it is the piece that kept failing. The gate was, in effect, reporting a problem that only the gate had.<br><br>Fix: the gate moves out of the notebook kernel into `research/scripts/check_gpu_env.py` and is invoked as a subprocess, so it validates *the environment the training actually uses* rather than the kernel's stale view of it. No mid-notebook session restart is needed, and "Run All" stays a single pass. It is launched via `subprocess.run` with an explicit `raise` on non-zero exit rather than a bare `!python`, because a `!` command's exit status does **not** halt Run All — a failed gate would otherwise scroll past and the smoke test would start anyway. Verified: the same poisoned process that fails in-process launches a subprocess that succeeds.<br><br>The gate also now reports **every** check with a per-item remedy instead of dying on the first assertion. That incidentally removes the exposure to the IPython traceback-formatter crash seen alongside this failure (`AttributeError: 'tuple' object has no attribute 'f_lineno'` in `ultratb._fixed_getinnerframes`), since the gate no longer depends on an exception rendering correctly to communicate. **That IPython crash was not independently reproduced here** — it needs Kaggle's exact IPython/CPython build — so it is recorded as consistent-with-a-known-formatter-bug and cosmetic (the underlying `SystemExit` message printed fine), not as a confirmed diagnosis.<br><br>One further correction from testing: a mismatched numpy raises **`AttributeError` as well as `ImportError`** depending on which submodule is reached first, so the gate catches both. The `--force-reinstall` step from M4-4 is **kept** — it is cheap and still the right remedy for a genuinely partial on-disk install, which is indistinguishable from this failure when observed from inside a stale kernel. **No pin changed; M4-2 and M4-4's pins stand.** `NOTEBOOK_REVISION` 6 → 7. A read-only diagnostic, `research/scripts/diagnose_numpy_env.py`, is committed alongside: it distinguishes stale-import from shadow-install from corrupt-install directly, so this question is settled by evidence next time rather than by inference. |
| M4-6 | 2026-08-20 | **Kaggle draft-session output is not durable; results must land on HF before a run is considered done.** Milestone 4's six training runs all completed and all six checkpoints reached their staging repos — and every metrics file was lost anyway. Environment/process bug, no research methodology touched. | The revision-7 Run All finished all 6 runs (mBERT + XLM-R × seeds 42/123/2026), each printing `checkpoint: {'pushed': True, ...}` with a repo id and revision SHA, and the packaging cell reported `zipped N files -> milestone4_metrics.zip`. The session then went idle and the interactive kernel stopped. Repeated attempts to download the zip from the Output panel failed silently — Kaggle's own endpoints were returning 503s at the time, so plausibly platform-side. Before a retry was possible the tab was reloaded and **`/kaggle/working` was empty**: no zip, no `repo/`, nothing. Nothing had been committed via "Save Version", so the metrics JSONs, which only ever existed on that ephemeral disk, were gone.<br><br>**The cause is an asymmetry in the pipeline, not bad luck.** Checkpoints were pushed **per seed**, with the notebook's own comment saying this was so "an interrupted session does not lose completed work". Metrics were packaged **once, at the very end**. The expensive artefact had the strong durability guarantee; the cheap artefact — the one that *is* the research output, and the one that cannot be regenerated without GPU time — had the weak one. Both were on the same disk, but only one had been copied off it before the disk disappeared.<br><br>Aggravating factor worth recording: the failure was **silent in both directions**. The download failed without an error the user could act on, and the notebook had already printed a success line for the packaging step, so the run *looked* complete. | **CLOSED as an environment/process fix; `CLAUDE.md` rule 3 not engaged — no experiment, config, seed or metric definition changed.**<br><br>**Recovery: eval-only re-scoring, not a retrain.** Chosen for fidelity before cost. Both configs set `load_best_model_at_end: true` and `push_checkpoint` uploads `trainer.model` after training, so **the checkpoint on the Hub is the exact model that produced the lost metrics**; scoring is `argmax` over logits with no sampling, so re-running it reproduces them. A retrain would not: GPU training is not bit-deterministic, so seed 42 retrained yields different weights, and pushing it would overwrite the seed branches and orphan the revision SHAs already recorded as pushed — leaving metrics that correspond to no artefact in hand. `REPRODUCIBILITY.md` Section 6 already makes the Hub checkpoint plus revision the canonical artefact, so re-scoring it is precisely what an external verifier would do. It is also strictly dominant as a gamble: if any push had silently failed, re-scoring fails for that seed alone and names it, scoping a retrain to 1/6 of the work.<br><br>Implemented as `research/src/models/evaluate_checkpoint.py`, which imports the scoring, dataset construction, label mapping, truncation stats and record builder from `transformer.py` rather than reimplementing them, so recovered records are identical in shape and derivation to training-run ones and need no special case downstream. `research/scripts/inventory_staging_checkpoints.py` is Step 0: it verifies all six branches carry config **and** weights **and** tokenizer (a branch with weights but no tokenizer is not re-scorable) before any GPU time is spent.<br><br>**What re-scoring cannot recover is recorded as `null`, never invented (`CLAUDE.md` rule 2): `train_runtime_seconds` and `train_loss`**, which are only observable while training, plus the training run's hardware. Each recovered file carries a `run_metadata.evaluation_only_recovery` block naming the source repo, branch and revision SHA, listing the unavailable fields, and stating that `run_metadata.hardware` describes the **evaluation** host rather than the training host. **None of these feed RQ1, RQ2 or RQ3** — Rehman accepted the trade-off explicitly. Everything that does feed them is recovered exactly: all metrics, confusion matrix, per-class figures, `prediction_collapse`, the majority-class baseline, `truncation` (recomputed deterministically) and the config.<br><br>**Prevention, which is the part that matters more than the recovery.** Metrics now go to the Hub **as each seed finishes**, via `research/src/evaluation/results_push.py`, into a dataset repo `<HF_STAGING_PREFIX>/urdu-misinfo-results-staging` — raw JSONs uploaded individually, not only as a zip, so each is separately fetchable and one bad upload cannot take the rest with it. Wired into both the training path and the recovery path. The push **never raises**: a failed upload must not destroy a finished training run, which would be strictly worse than the problem being solved; it retries with backoff and reports failure in its return value. Its counterpart `verify_results_uploaded()` **is** allowed to fail a run and **fails closed** — an unreachable Hub reads as unverified, never as verified. The notebook's packaging cell now pushes, verifies against what the Hub actually lists, and **stops the notebook if it cannot confirm**, before building the zip. The Output tab is demoted to a convenience.<br><br>Two incidental fixes found while doing this: the summary cell computed `train_runtime_seconds / 60` and would have **crashed** on a recovered file (`.get(key, 0)` does not help — the key exists holding `None`, so the default never applies); it now prints `n/a` and flags recovered rows so one is never mistaken for a training run. `NOTEBOOK_REVISION` 7 → 8. **The standing rule from here: a milestone's results are not "produced" until they are off the session disk.** |
| M5-1 | 2026-08-20 | **Experiment G is unimplementable as specified: the blueprint both requires and forbids training on Notri-Fact. RESOLVED 2026-08-20 (option (a), "full G later") — DEFERRED/SCHEDULED, pending GPU time. F is implemented, G is not yet.** | `EXPERIMENT_PLAN.md` Section 2 step 4 and `MASTER_PROJECT_BLUEPRINT.md` Part 11 (line 237) specify **G = "Cross-dataset: train Notri-Fact → test Ax-to-Grind (zero-shot)", all four models**, and Part 31 figure 7 makes the 4-model × 2-direction heatmap the RQ2 headline. But Part 9 (line 205) states, of the same corpus: **"Cross-dataset test (Notri-Fact): used in full, held out entirely — no portion of Notri-Fact is used for training or hyperparameter tuning at any point, to keep RQ2's zero-shot claim valid."** G requires training on Notri-Fact; Part 9 forbids using any portion of it for training. Both cannot hold. `research/configs/data.yaml` sides unambiguously with Part 9: Notri-Fact carries **only** a `holdout` split (13,355 rows — no train/val/test), is marked **`train_on: false`**, and is annotated *"R7: never trained on, never split, never concatenated with Ax-to-Grind."* Two further practical blockers follow from the same root: (1) **no Notri-Fact train split exists**, so G needs a Milestone-2 data-design change (splitting a corpus the plan says is never split), and (2) for C and D, G needs **transformer checkpoints trained on Notri-Fact**, which do not exist — that is a full Milestone-4-scale training job (6 runs), not the inference-only pass F needs. The task framing "load the existing trained checkpoints and evaluate zero-shot" therefore only works for F; for G there is nothing to load. Note R7 itself is narrower than data.yaml's gloss: R7 forbids **concatenation** ("always stay separate train/test pools"), not training on each separately — so the binding prohibition is Part 9's sentence and data.yaml's flag, not R7 as literally written. | **DECISION REQUIRED — Rehman, before Milestone 5 can be called complete.** Nothing has been guessed: `run_cross_dataset.py`'s `DIRECTIONS` table contains **only F**, and a test asserts `G` stays absent and that no `G_*.json` appears, so the contradiction cannot be silently resolved by an accidental implementation producing a headline RQ2 number that rests on it. Why this affects validity rather than tidiness: RQ2's stated headline is a **bidirectional** transfer matrix, and Part 13's hypothesis is explicitly about an **asymmetry** ("substantial F1 drop... and vice versa in the opposite (milder) direction"). With only F, the asymmetry claim cannot be made at all — one direction measures a drop, it does not measure that the drop is direction-dependent. Options: **(a)** implement G as specified — split Notri-Fact (stratified, dedup already applied), train all four models on it, evaluate on Ax-to-Grind test; this contradicts Part 9's "held out entirely" sentence, so Part 9 must be amended here and the zero-shot claim restated as "zero-shot **with respect to the evaluated direction**", which is the standard reading in transfer work and is defensible — cost: a new split plus 6 transformer training runs; **(b)** drop G and restate RQ2 as a **single-direction** transfer result, amending Part 11, Part 13's asymmetry hypothesis and Part 31 figure 7 (heatmap becomes 4×1) — cheapest, but forfeits the asymmetry finding that Part 13 says replicates Haroon's; **(c)** implement G for the **classical models only** (A, B), which need no checkpoints and cost seconds, and report the transformer half of G as out of scope — gives a partial asymmetry signal at near-zero cost, but a 2-model × 2-direction matrix is not what Part 31 figure 7 specifies. Recommended: **(a)** if the GPU budget allows, since the asymmetry is a stated contribution and (b) permanently removes it; **(c)** as a cheap interim that makes the asymmetry measurable at all while (a) is scheduled. Not acted on.<br><br>**RESOLVED 2026-08-20 — Rehman chose option (a), "full G later". DEFERRED/SCHEDULED, not open, and not implemented in this pass.** The contradiction is settled in principle: **`MASTER_PROJECT_BLUEPRINT.md` Part 9 will be amended to permit a train/test split of Notri-Fact specifically for Experiment G**, so that G can run as Part 11 and `EXPERIMENT_PLAN.md` step 4 specify, with all four models and both directions, preserving Part 13's asymmetry hypothesis and Part 31 figure 7's 4-model × 2-direction heatmap. The zero-shot claim survives because it is a claim about the *evaluated* direction: a model trained on Notri-Fact and evaluated on Ax-to-Grind has still never seen Ax-to-Grind, and vice versa. What is given up is Part 9's stronger "no portion of Notri-Fact is used for training at any point", which was written for the single-direction reading of RQ2.<br><br>**Pending implementation, gated on GPU availability.** Blocked work, none of it started: (1) a stratified Notri-Fact train/test split — deduplication is already applied to that corpus (33 rows removed, `duplication_report.json`), so the split is straightforward, but the methodology (fractions, stratification, whether the existing 13,355-row `holdout` becomes the test side or is re-split) is deliberately **not decided here**; (2) `research/configs/data.yaml` flipping `train_on: false` and gaining the new split paths; (3) **6 additional transformer training runs** — mBERT and XLM-R × seeds {42, 123, 2026} on the Notri-Fact split, i.e. a second Milestone-4-scale GPU job; (4) the classical half of G, which is cheap and needs no checkpoints; (5) `run_cross_dataset.py`'s `DIRECTIONS` table gaining a `G` row.<br><br>**Part 9's text is deliberately NOT amended yet.** The amendment happens in the session that works out the Notri-Fact split methodology, not silently now — editing the constraint before the design that justifies it exists would leave the spec permitting something no artefact implements. Until then the guard stands: `DIRECTIONS` contains only `F`, and `test_experiment_g_is_not_silently_implemented` asserts `G` stays absent and that no `G_*.json` appears, so a headline RQ2 number cannot be produced against a half-resolved decision. **Milestone 5 cannot be called complete until G lands**, since `ROADMAP.md`'s acceptance criteria require the full REQUIRED set. |
| M4-7 | 2026-08-20 | **The XLM-R staging checkpoint repo is PUBLIC despite both configs specifying `private: true`. Reported, not changed — no credentials in this environment to change it.** | Verified by anonymous access with no token: `RehmanAyoub/urdu-misinfo-xlmr-staging` returns `private=False` and is fully readable (all four branches — `main`, `seed-42`, `seed-123`, `seed-2026` — enumerable anonymously), while `RehmanAyoub/urdu-misinfo-mbert-staging` and `RehmanAyoub/urdu-misinfo-results-staging` both correctly return **401**. Both `research/configs/model_xlmr.yaml` and `model_mbert.yaml` set `checkpoint_push.private: true`, and `push_checkpoint` passes it to `api.create_repo(..., private=..., exist_ok=True)`. The cause is a silent no-op in that call: **`create_repo(exist_ok=True)` does not modify the visibility of a repo that already exists.** If the XLM-R repo was created before that flag was in place (or by hand), every later push left it public and printed nothing to say so. | **Closed as reported; Rehman's call, and it needs his token either way.** Low severity — the checkpoints are fine-tuned from public base models on public datasets, and contain no credentials or private data — so this is a stated-intent mismatch rather than a leak. But it matters for two reasons: the repo is *staging*, and `EXPERIMENT_PLAN.md` Section 7 makes promotion to production a deliberate Milestone 9 decision, so a publicly discoverable staging checkpoint invites exactly the "is this the real model?" confusion that split is meant to prevent; and the mismatch is silent, so any future repo created before its config settles will inherit the same state undetected. Fix is one action in the HF settings UI (Settings → Change visibility → Private), or `HfApi().update_repo_visibility(repo_id, private=True)` with a write token. Worth noting the side effect if it is flipped: the eval-only recovery and the Experiment F transformer path currently work anonymously against XLM-R **because** it is public; once private they will require `HF_TOKEN`, which is the correct state but should not be discovered mid-run. Deliberately not "fixed" by loosening the config to `private: false` — that would encode the accident as the intent. |
