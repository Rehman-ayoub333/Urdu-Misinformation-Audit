// Registers jest-dom's matchers (toBeInTheDocument, etc.) with Vitest's expect,
// including their type augmentations. Loaded via vitest.config.ts setupFiles.
import "@testing-library/jest-dom/vitest";
